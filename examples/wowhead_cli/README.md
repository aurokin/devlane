# wowhead_cli example adapter

This example shows how a CLI-heavy repo can still adopt the lane contract.

The emphasis here is on:

- stable ownership of global wrappers
- branch-local editable environments
- generated shell activation
- XDG and runtime roots
- optional cache sidecars
